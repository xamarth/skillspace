interface:
  display_name: "Skillspace"
  short_description: "Repo-specific patterns and workflows for skillspace"
  default_prompt: "Use the skillspace repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true